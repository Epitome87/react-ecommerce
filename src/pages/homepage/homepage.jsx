import React from 'react';
import Directory from '../../components/directory/Directory';
import * as Styled from './HomePageContainer';

const HomePage = (props) => {
  return (
    <Styled.HomePage>
      <Directory />
    </Styled.HomePage>
  );
};

export default HomePage;
